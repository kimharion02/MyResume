function toggleSidebar() {
    var sidebar = document.querySelector('.menu'); // Update this selector to match your menu class
    sidebar.style.width = (sidebar.style.width === '300px') ? '0' : '300px'; // Adjust the width as needed
}
